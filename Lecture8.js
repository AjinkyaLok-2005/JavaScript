let a = 10;
//this will have a memory assigned but it will be in other memory space 
//rather than the global memory space
//AND we can not access this memory space untill we put some value in them

console.log(a);
var b = 100;
